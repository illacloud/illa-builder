import { css } from "@emotion/react"
import { globalColor, illaPrefix } from "@illa-design/theme"

export const configBodyStyle = css`
  width: 100%;
  border-top: 1px solid ${globalColor(`--${illaPrefix}-grayBlue-08`)};
`
