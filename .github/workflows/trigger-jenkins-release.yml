name: jenkins-illa-builder-release
on:
  push:
    branches:
    - 'releases/**'
  pull_request:
    branches:
    - 'releases/**'
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: trigger single Job
      uses: appleboy/jenkins-action@master
      with:
        url: "http://ec2-18-181-186-207.ap-northeast-1.compute.amazonaws.com:9000/"
        user: "illasoft"
        token: ${{ secrets.JENKINS_API_TOKEN }}
        job: "illa-builder-release"
