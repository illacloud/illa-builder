import { FC } from "react"
import { BaseDataGridProps } from "./interface"

export const DataGridCommunityWidget: FC<BaseDataGridProps> = (props) => {
  const {} = props
  return <div></div>
}

DataGridCommunityWidget.displayName = "DataGridCommunityWidget"
export default DataGridCommunityWidget
