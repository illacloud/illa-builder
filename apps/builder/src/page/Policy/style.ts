import { css } from "@emotion/react"

export const policyContainer = css`
  font-size: 16px;
  margin: auto;
  max-width: 600px;
  padding: 48px;
  @media (max-width: 640px) {
    padding: 30px;
    font-size: 14px;
  }
`
